# ---------------------------------------------------------------------------
# Sales API — server environment
#
# WHAT THIS IS. Read at RUNTIME on every request, unlike the React build, so it
# lives on the server and nowhere else.
#
# HOW TO USE IT. Copy this file to .env in the same folder, once per host, and
# fill in the blanks:
#
#     cd <document root>/api && cp .env.example .env
#
# The deploy ships THIS file on every run, and never ships or deletes your .env
# — it is excluded from the rsync in both directions. Neither is readable over
# HTTP: the .htaccess beside them answers 404 to every dotfile.
#
# Values here are examples, not defaults to keep. Anything marked REQUIRED has
# no working fallback; the API answers 503 without it.
# ---------------------------------------------------------------------------


# --- Identity --------------------------------------------------------------

# production | sandbox | local. Reported by GET /api/health, which is how you
# tell two deployments apart when you are looking at one of them.
APP_ENV=production

# This product's name. It is sent to the other products as the caller identity
# and it prefixes every idempotency key this app mints, so changing it on a
# live host makes in-flight retries look like new requests. Leave it alone.
APP_PRODUCT_KEY=sales


# --- Database (REQUIRED) ---------------------------------------------------
#
# The Sales database and nothing else. This app never connects to another
# product's database — it asks that product over HTTP — so these credentials
# should grant access to this one schema and no more.

DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=sales_prod
DB_USER=
DB_PASS=

# Optional. Set only if the tables do not live in "public".
# DB_SCHEMA=public


# --- Talking to the other products -----------------------------------------
#
# The base URLs are DERIVED FROM THIS HOST and normally need nothing here:
# a request arriving at sales.aicountly.com calls the other products'
# production hosts, and one arriving at sales.gh.aicountly.com calls their
# sandboxes. That is why there is no second set of URLs to keep in step.
#
# Set one only to point somewhere else — a local instance, or a cutover.
# BOOKS_API_BASE=https://books.aicountly.com
# INVENTORY_API_BASE=https://inventory.aicountly.com
# MANAGE_API_BASE=https://manage.aicountly.com

# Contacts does not exist in every deployment. With neither of these set, the
# app degrades honestly rather than erroring: customer lookups fall back to
# Books. Set CONTACTS_API_BASE when it is available.
# CONTACTS_API_BASE=https://contacts.aicountly.com
# CONTACTS_ENABLED=1


# --- Service keys ----------------------------------------------------------
#
# OUTBOUND: the key this app presents when it calls Books or Inventory as
# itself rather than as the signed-in user — posting an invoice, relieving
# stock. Issued by that product. Without them those calls are rejected and
# documents sit in FAILED, visible on the screen they belong to.

BOOKS_SERVICE_KEY=
INVENTORY_SERVICE_KEY=

# INBOUND: keys this app ACCEPTS from its siblings, as app:key pairs. Only
# needed if another product calls this one. Give each product its own key so a
# leak can be traced and rotated on its own.
# SERVICE_KEYS=books:<key>,inventory:<key>,pos:<key>


# --- AI (optional) ---------------------------------------------------------
#
# The dashboards' suggestions are produced by RULES in this product — every
# threshold, count and ranking is arithmetic over records the user can open,
# and none of it needs a provider. With these set, a configured provider may
# additionally rephrase the WORDING of a suggestion this product has already
# produced. It never invents a suggestion, never supplies a figure and never
# chooses an action.
#
# Provider and key GOVERNANCE lives in console.aicountly.org. The call is made
# server-side from here; no key ever reaches the browser, and there is no
# VITE_ equivalent of either of these on purpose.
#
# Leave both empty and every suggestion still works, labelled "Rule-based
# insight" — which is what it has been all along.

# AI_PROVIDER_BASE=
# AI_PROVIDER_KEY=


# --- Portal and CORS -------------------------------------------------------

# The AICOUNTLY portal serves BOTH production and sandbox from my.aicountly.com
# (sandbox.aicountly.com is a login redirect only — it serves neither seskey nor
# validatesession). Set this only for a non-standard portal.
# PORTAL_AUTH_BASE=https://my.aicountly.com

# Both deployed environments are same-origin with their own app, so this stays
# empty in production. It exists so `npm run dev` can call a deployed API.
# CORS_ALLOWED_ORIGINS=http://localhost:5173
